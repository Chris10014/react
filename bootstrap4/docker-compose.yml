version: '3.4'

services:
  confusion:
    image: confusion
    build:
      context: conFusion
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: production
    ports:
      - 3000:3000
